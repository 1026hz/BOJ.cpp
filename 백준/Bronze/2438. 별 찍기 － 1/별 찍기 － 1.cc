#include <bits/stdc++.h>
using namespace std;

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    int n=0;
    cin >> n;
    string s;
    
    for(int i=1; i<=n; i++){
        s += "*";
        cout << s << '\n';
    }

}